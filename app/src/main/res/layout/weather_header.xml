<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipToPadding="false"
    >
    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/tv_weather_temperature"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:textSize="96sp"
        android:fontFamily="sans-serif-thin"
        android:shadowColor="#000"
        android:shadowRadius="8"
        android:shadowDy="2"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="30°"
        />
    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/tv_weather_description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:textSize="48sp"
        android:fontFamily="sans-serif-thin"
        android:shadowColor="#000"
        android:shadowRadius="8"
        android:shadowDy="2"
        app:layout_constraintStart_toEndOf="@+id/tv_weather_temperature"
        app:layout_constraintBaseline_toBaselineOf="@+id/tv_weather_temperature"
        tools:text="Облачно"
        />
    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/tv_weather_subline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:textSize="24sp"
        android:fontFamily="sans-serif-thin"
        android:shadowColor="#000"
        android:shadowRadius="8"
        android:shadowDy="2"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tv_weather_temperature"
        tools:text="Тбилиси, ветер 5м/с"
        />

</androidx.constraintlayout.widget.ConstraintLayout>